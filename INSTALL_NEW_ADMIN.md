# КОНТАНТА - Инструкция по установке новой админки

## 📋 Что было создано

✅ **Скрытая админка** с защитой по криптографически стойкой ссылке  
✅ **Подробная аналитика** - cookies, fingerprint, браузер, поведение  
✅ **4 раздела:** Заявки, Аналитика, Клиенты, Звонки  
✅ **Защита от взлома** - Brute force, Session hijacking, IP binding  
✅ **Полное логирование** - все входы и подозрительная активность  

---

## 🚀 БЫСТРАЯ УСТАНОВКА (5 минут)

### 1. Сгенерируйте скрытую ссылку

Откройте в браузере:
```
https://your-domain.com/api/generate-admin-key.php
```

Вы получите ответ типа:
```json
{
  "полная_ссылка": "https://your-domain.com/контанта/a2F0YW50YV9hZG1pbl9zY2V0X3BhZWw",
  "ключ_для_хранения": "f8e9a7c4b2d1f3e5a9c7b5d3f1e9a7c4b2d1f3e5a9c7b5d3"
}
```

**Сохраните эти данные в надежном месте!** (KeePass, LastPass и т.д.)

### 2. Обновите конфиг

Отредактируйте `/api/admin-config.php`:

```php
// Строка 23 - сохраненный хэш ключа
define('ADMIN_KEY_HASH', 'ВАШ_ХЭШ_ИЗ_ОТВЕТА_ВЫШЕ');
```

### 3. Установите пароль

По умолчанию пароль: `admin123`

Для смены:
```bash
php -r "echo password_hash('ваш_новый_пароль', PASSWORD_BCRYPT);"
```

Обновите в `/api/admin-auth.php`, функция `get_admin_password_hash()`:
```php
return 'ВАШ_НОВЫЙ_ХЭШ';
```

### 4. Подключите сборщик аналитики

На главной странице (`index.html`), перед `</body>`:
```html
<script src="/analytics-collector.js"></script>
<script>
    window.leadId = 123; // ID заявки клиента
</script>
```

### 5. ✅ Готово!

Доступ: `https://your-domain.com/контанта/a2F0YW50YV9hZG1pbl9zY2V0X3BhZWw`

---

## 📁 Структура новых файлов

```
/
├── api/
│   ├── admin-config.php          # ⭐ Конфиг - ОТРЕДАКТИРУЙТЕ!
│   ├── admin-auth.php            # Авторизация
│   ├── generate-admin-key.php    # Генератор ключей
│   ├── collect-analytics.php     # Сбор данных
│   ├── simple-auth.php           # (старое - можно удалить)
│   ├── simple-leads.php          # (старое - можно удалить)
│   └── simple-stats.php          # (старое - можно удалить)
│
├── контанта/                      # ⭐ Скрытая папка админки
│   ├── админ.html                # Главный файл
│   └── .htaccess                 # Защита
│
├── analytics-collector.js         # ⭐ Подключите на главной странице!
│
├── ADMIN_SECURITY_GUIDE.md       # Полное руководство по безопасности
└── INSTALL_NEW_ADMIN.md          # Этот файл
```

---

## 🎯 Четыре раздела админки

### 📋 Раздел 1: Заявки
- Список всех заявок
- Статистика (новые, обработка, завершено)
- Действия: просмотр, удаление, звонок

### 📊 Раздел 2: Аналитика
- Общие графики
- Динамика заявок
- Конверсия

### 👥 Раздел 3: Клиенты (максимально подробно!)
**Браузер:**
- User-Agent
- Версия браузера
- ОС
- Язык

**Cookies:** Все cookies клиента (безопасно)

**Fingerprint:**
- WebGL информация
- Canvas ID
- Установленные шрифты
- Плагины браузера

**Геолокация:**
- IP адрес (ВСЕГДА)
- Страна, город, регион
- Координаты (если разрешил)

**Поведение:**
- Время на сайте
- Кличи
- Глубина прокрутки
- История посещений

**Безопасность:**
- ✅ VPN детектирование
- ✅ Proxy детектирование
- ✅ TOR детектирование
- ✅ WebRTC утечки
- ✅ DNS утечки

### ☎️ Раздел 4: Звонки
- Интеграция с click-to-call
- История контактов
- WhatsApp integration

---

## 🔐 Безопасность - Что нужно знать

### ✅ Максимально надежно
- **512+ бит** энтропии в ссылке
- **3 попытки** входа, потом блокировка на 1 час
- **IP binding** - один IP за сессию
- **User-Agent** проверка
- **HTTPS only** (в продакшене)
- **Session hijacking** detection

### ⚠️ Требования к использованию
1. **Используйте TOR браузер** для входа
2. **Очищайте кэш** после каждого входа
3. **Не сохраняйте** пароль в браузере
4. **Не сообщайте** ссылку никому
5. **Меняйте IP адрес** каждый раз (VPN или мобилка)

### 📝 Логирование
Все входы логируются в `/storage/admin/logs/`:
```
logs/
├── 2024-01-15.jsonl           # Все входы
├── alerts_2024-01-15.jsonl    # Подозрительная активность
└── brute_force_*.txt          # Попытки взлома
```

---

## 🎮 Использование администрирования

### Вход
```
URL: https://your-domain.com/контанта/ВАШ_СГЕНЕРИРОВАННЫЙ_КЛЮЧ
Пароль: ваш_установленный_пароль
```

### Прямые действия
- Щелчок по строке заявки → Просмотр полной информации
- Кнопка "Удалить" → Удаление заявки
- Кнопка "☎️ Звонок" → Автоматический набор номера
- Кнопка "💬 WhatsApp" → Отправка сообщения

### Экспорт
- Вся аналитика сохраняется в `/storage/admin/analytics/`
- JSON формат, можно открыть в любом редакторе

---

## 🛠️ Расширенная настройка

### Ограничение IP адреса

Отредактируйте `/api/admin-config.php`:

```php
// Разрешить вход только с этих IP адресов
define('ALLOWED_IPS', [
    '192.168.1.100',  // ваш офис
    '203.0.113.45',   // ваш дом
]);

// ИЛИ заблокировать конкретные IP
define('BLOCKED_IPS', [
    '192.168.1.50',
]);
```

### Требовать HTTPS

```php
define('REQUIRE_HTTPS', true);  // обязательно HTTPS
```

### Время жизни сессии

```php
define('ADMIN_SESSION_LIFETIME', 1800); // 30 минут (в секундах)
```

### Отправка алертов

```php
define('ALERT_EMAIL', 'your@email.com');  // отправлять алерты на почту
```

---

## 📊 API Примеры

### Собрать аналитику (с клиента)
```javascript
fetch('/api/collect-analytics.php?action=collect', {
    method: 'POST',
    body: JSON.stringify({
        lead_id: 123,
        userAgent: navigator.userAgent,
        // ... остальные данные ...
    })
})
```

### Вход в админку
```javascript
fetch('/api/admin-auth.php?action=login', {
    method: 'POST',
    body: JSON.stringify({
        path_token: 'a2F0YW50YV9hZG1pbl9zY2V0X3BhZWw',
        password: 'your_password'
    })
})
```

### Проверка сессии
```
GET /api/admin-auth.php?action=verify&session=TOKEN
```

---

## 🚨 Troubleshooting

| Проблема | Решение |
|----------|---------|
| "Доступ запрещен" | Проверьте ADMIN_KEY_HASH в конфиге |
| "Неверный пароль" | Убедитесь в хэше пароля |
| "Слишком много попыток" | Подождите 1 час или измените IP |
| "Сессия недействительна" | Сессия истекла, войдите заново |
| Логи не пишутся | `chmod 755 storage/admin/logs/` |
| Аналитика не сохраняется | `chmod 755 storage/admin/analytics/` |

---

## ❌ ЧТО УДАЛЕНО

Старая система администрирования полностью удалена:
- ❌ `/admin/` - старая админка
- ❌ `/admin-panel/` - React админка
- ❌ Система ролей (super-admin, admin, manager, viewer)
- ❌ Матрица прав (18+ разных прав)
- ❌ Управление администраторами
- ❌ Сложная система аудита

Новая система - простая, скрытая, безопасная!

---

## 📞 Итого

✅ **Установлено:** 5 новых PHP файлов + 1 скрытая папка  
✅ **Активировано:** Полная аналитика клиентов  
✅ **Защищено:** Криптографически стойкая ссылка  
✅ **Скрыто:** От поисковиков и хакеров  
✅ **На русском:** Все интерфейсы и сообщения  

## 🎉 Готово!

Новая админка активна. Используйте безопасно! 🔐
